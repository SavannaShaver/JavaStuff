import java.util.Scanner;
import java.util.Random;
import java.util.*;
import java.io.*;
public class Showcase {
	
	private Prize[] prizes;
	public static final String FILE_NAME = "./prizeList.txt"; 
	public static final String DELIM = "\t"; 
	//public static final int DEF_SIZE = 10;
	//String[] words = readFile(FILE_NAME);
	
	//public void readPrizeFile(String aN)
	//{
		//try
		{
			//Scanner fileScanner = new Scanner(new File(aN));
			//String fileLine = fileScanner.nextLine();
			//String splitLines = fileLine.concat(split(DELIM));

			//while(fileScanner.hasNextLine())
			{
				//fileLine = fileScanner.nextLine();
				//String name = splitLines[0];
				//String price = splitLines[1];
				//Prize aPrize = new Prize(name,price);
			}
		
			//System.out.println(pName);
		}
	
	}
	
//}


